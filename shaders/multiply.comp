#version 450

layout(local_size_x = 16) in;

// Input/output buffer
layout(set = 0, binding = 0) buffer Data {
    uint values[];
}pero;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    pero.values[idx] = 3;
    uint aa = pero.values[idx] + 2;
    aa = 99;
    pero.values[idx] = 99;
}
